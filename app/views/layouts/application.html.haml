!!!
%html
  %head
    %title 
      Resolute Lacrosse
    = stylesheet_link_tag :all
    = javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.1/jquery.js", "#{Rails.root}/public/javascripts/video.js", :ckeditor, :defaults, "jquery-ui"
    = csrf_meta_tag
  %body
    = render :partial => "items/popup", :locals => {:source => "#{Rails.root}/public/videos/mirror.avi" }            
    
    #container
      #header 
        %span.logo 
          = image_tag "logo.png"
        %br
        %span.tagline 
          Never Stop. Never Quit. Be Resolute.
        #nav
          %ul.nav_links
            - Category.list_order.each do |category|
              %li.first_level
                = (only_whitespace?(category.body) && category.title != "Home") ? category.title : (link_to category.title, pretty_url_path(category))
                %ul.second_level
                  - category.pages.each do |page| 
                    %li
                      = link_to page.title, pretty_url_path(page)
            %li.first_level= link_to "Sign Out", destroy_user_session_path if user_signed_in?
      #content<
        - if flash.any?
          #flash
            - flash.each do |key, message|
              %span{ :class => key }= message

        = yield
