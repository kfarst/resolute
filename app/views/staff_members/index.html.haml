#title Staff Members

- @staff_members.each do |member|
  #staff-member.clear
    #profile-picture
      = link_to (image_tag member.profile_picture.url(:thumb)), member.profile_picture.url(:medium), :rel => "vidbox"
    #member-content
      #staff-member-name= member.name
      %br/
      %ul.staff-stats
        - member.stats_array.each do |stat|
          %li= stat
      %br/
      = raw member.bio
%br/
